import { DraggableCardDirective } from './draggable-card.directive';

describe('DraggableCardDirective', () => {
  it('should create an instance', () => {
    const directive = new DraggableCardDirective();
    expect(directive).toBeTruthy();
  });
});
