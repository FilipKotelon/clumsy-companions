import { FoldedCardsDirective } from './folded-cards.directive';

describe('FoldedCardsDirective', () => {
  it('should create an instance', () => {
    const directive = new FoldedCardsDirective();
    expect(directive).toBeTruthy();
  });
});
